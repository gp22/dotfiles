- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: 
      force: true
    ~/.zshrc:
      force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
